// Filtre paneli — solda görünür
// Filtreler: tür (hoca/ders/bölüm), bölüm, sıralama (puan/yorum sayısı/isim)
// nuqs ile URL'e yansır
export default function FilterPanel() {
  return <div>FilterPanel — yapım aşamasında</div>;
}
