// Ders özet kartı — listede gösterim için
// İçerik: ders adı, kodu, hoca adı, ortalama puan, yorum sayısı
export default function CourseCard() {
  return <div>CourseCard — yapım aşamasında</div>;
}
