// Yorum yazma formu
// Alanlar: anlatım kalitesi (1-5), ders zorluğu (1-5), sınav zorluğu (1-5),
//          katılım zorunlu mu (evet/hayır), yorum metni (maks 500 karakter)
// Tüm yorumlar otomatik anonim gönderilir
export default function ReviewForm() {
  return <div>ReviewForm — yapım aşamasında</div>;
}
